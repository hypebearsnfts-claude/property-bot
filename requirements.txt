# Telegram bot framework
python-telegram-bot==21.6

# Web scraping
playwright==1.49.0
playwright-stealth==1.0.6
beautifulsoup4==4.12.3
lxml==5.2.2

# HTTP clients
requests==2.32.3
aiohttp==3.9.5
# Chrome TLS/JA3 impersonation — lets the Zoopla/OpenRent scrapers pass WAF
# fingerprint checks from the cloud without a browser. 0.15 adds modern targets
# (chrome131/136/142, safari18) which the scrapers rotate through. Falls back to
# requests. NOTE: defeats fingerprinting, NOT datacenter-IP reputation blocks —
# if a site bans the runner's IP outright, set ZOOPLA_PROXY / OPENRENT_PROXY.
curl_cffi==0.15.0

# AI / LLM
anthropic>=0.40.0

# Environment & config
python-dotenv==1.0.1
