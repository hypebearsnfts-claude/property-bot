# Telegram bot framework
python-telegram-bot==21.6

# Web scraping
playwright==1.49.0
playwright-stealth==1.0.6
beautifulsoup4==4.12.3
lxml==5.2.2

# HTTP clients
requests==2.32.3
aiohttp==3.9.5
# Chrome TLS/JA3 impersonation — lets the Zoopla scraper pass Cloudflare's
# fingerprint check from the cloud without a browser. Falls back to requests.
curl_cffi==0.7.4

# AI / LLM
anthropic>=0.40.0

# Environment & config
python-dotenv==1.0.1
